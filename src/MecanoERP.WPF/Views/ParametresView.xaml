<UserControl x:Class="MecanoERP.WPF.Views.ParametresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0D0D1A">
    <StackPanel Margin="28">
        <TextBlock Text="⚙ Paramètres" FontSize="24" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,0,20"/>
        <Border Background="#1E1E35" CornerRadius="12" Padding="28">
            <StackPanel>
                <TextBlock Text="Configuration du garage" FontSize="16" FontWeight="SemiBold" Foreground="#4FC3F7" Margin="0,0,0,16"/>
                <TextBlock Text="Nom du garage" Foreground="#8899AA" Margin="0,0,0,4"/>
                <TextBox Text="Garage MecanoERP" Margin="0,0,0,12"/>
                <TextBlock Text="Adresse" Foreground="#8899AA" Margin="0,0,0,4"/>
                <TextBox Text="Québec, Canada" Margin="0,0,0,12"/>
                <TextBlock Text="Téléphone" Foreground="#8899AA" Margin="0,0,0,4"/>
                <TextBox Margin="0,0,0,20"/>
                <Button Content="💾 Sauvegarder" Style="{StaticResource SuccessBtn}" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
